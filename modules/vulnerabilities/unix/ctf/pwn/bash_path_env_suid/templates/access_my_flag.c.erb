#include <stdio.h>
#include <stdlib.h>
#include <sys/types.h>
#include <unistd.h>
#include <errno.h>

int main()
{
  printf("      	UID         	GID \n"
     	"Real  	%d  Real  	%d \n"
     	"Effective %d  Effective %d \n",
     	getuid (), 	getgid (),
     	geteuid(), 	getegid());

  system("<%= @secgen_parameters['bash_command'].first %> /home/<%= @username %>/flag");
  return EXIT_SUCCESS;
}
